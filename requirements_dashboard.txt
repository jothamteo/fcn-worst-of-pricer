# Dashboard runtime — installs on top of the base pricer requirements.
# Use:  pip install -r requirements.txt -r requirements_dashboard.txt
#
# Pinned light: lower bounds are picked so the dashboard works on any modern
# Python ≥ 3.10 wheel set; no upper bounds because Streamlit Community Cloud
# tracks the latest releases.

streamlit>=1.36
plotly>=5.22
pyarrow>=15.0
joblib>=1.4
